## 2024-05-23 - Tooltip Accessibility
**Learning:** To make tooltips accessible to keyboard users in Tkinter, bind to `<FocusIn>` and `<FocusOut>` events on the widget, not just `<Enter>` and `<Leave>`.
**Action:** Always include focus bindings when implementing hover-based UI elements.
